
// import { SortOptions } from './const.js';

const sort = {

  // [SortOptions.Popular]:(offers) => offers,
  // [SortOptions.PriceUp]:(offers) => offers.toSorted(sortByPriceUp),
  // [SortOptions.PriceDown]:(offers) => offers.toSorted(sortByPriceDown),
  // [SortOptions.ByRating]:(offers) => offers.toSorted(sortByRating),

};

// function getWeightForNull(pointA, pointB) {
//   if (pointA === null && pointB === null) {
//     return 0;
//   }

//   if (pointA === null) {
//     return 1;
//   }

//   if (pointB === null) {
//     return -1;
//   }

//   return null;
// }


// export function sortByPriceUp(pointA, pointB) {

//   return pointA.price - pointB.price;
// }

// export function sortByPriceDown(pointA, pointB) {

// return pointB.price- pointA.price;

// }

// export function sortByRating(pointA, pointB) {

//   return pointB.rating - pointA.rating;
// }

export {sort};
